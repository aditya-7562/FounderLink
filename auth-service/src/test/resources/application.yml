spring:
  application:
    name: auth-service-test
  cloud:
    config:
      enabled: false
  config:
    import: optional:configserver:http://config-server:8888
    import-check.enabled: false
  datasource:
    url: jdbc:h2:mem:authdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    enabled: false

server:
  port: 0

jwt:
  secret: VGhpc0lzQVN0cm9uZ0pXVFNlY3JldEtleUZvclRlc3RzMTIzNDU2Nzg5MDEy
  expiration: 3600000
  refresh-expiration: 604800000

seed:
  admin:
    enabled: false
    name: Admin
    email: admin@founderlink.com
    password: Password123
