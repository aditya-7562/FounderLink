spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: ${USER_DB_PASSWORD:Anim@4748}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  cloud:
    config:
      enabled: false

server:
  port: 8082

internal:
  secret: my-founderlink-internal-secret-2024

eureka:
  client:
    enabled: true